name: ipfs_node_flutter_web_example
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ipfs_node_flutter_platform_interface:
    path: ../../ipfs_node_flutter_platform_interface
  ipfs_node_flutter_web:
    path: ..

flutter:
  uses-material-design: true
